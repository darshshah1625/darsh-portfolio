// Profile content - WORLD'S BEST DEVELOPER EDITION
const content = {
  developer: {
    heroTitle: 'Pioneering the Future of AI-Driven Software Transformation',
    heroDescription: 'Elite SDE II @ AWS | Architect of the world\'s first agentic AI mainframe modernization platform | Transforming 20M+ LOC for Fortune 500',
    projectsTitle: 'Groundbreaking Projects',
    aboutContent: `
      <p>👋 I'm Darsh Shah - an elite Software Development Engineer II at Amazon Web Services, where I'm pioneering the future of enterprise software transformation.</p>
      <p><strong>What I Do:</strong> I architect and build cutting-edge multi-agent AI systems that autonomously modernize legacy enterprise applications. My work on AWS Transform has revolutionized how Fortune 500 companies approach mainframe modernization.</p>
      <p><strong>Technical Excellence:</strong> Master of distributed systems, cloud architecture, and agentic AI. I've designed production systems processing 20M+ lines of code, serving 23+ enterprise clients with 99.99% uptime.</p>
      <p><strong>Innovation:</strong> Created the first-ever Claude AI-powered code refactoring service at enterprise scale. Built A2A (Agent-to-Agent) protocols enabling real-time multi-agent orchestration. Reduced critical infrastructure recovery time by 98.8%.</p>
      <p><strong>Tech Stack Mastery:</strong> Java, Python, TypeScript, Scala | AWS (CDK, Lambda, Bedrock, ECS) | Claude AI, FastAPI | PostgreSQL, Oracle, DynamoDB | Docker, Kubernetes, Terraform | Apache Kafka, Spark, Airflow</p>
      <p><strong>Current Focus:</strong> Pushing the boundaries of LLM orchestration, building fault-tolerant distributed AI systems, and making the impossible possible at cloud scale.</p>
    `,
    icon: '👨‍💻'
  },
  recruiter: {
    heroTitle: 'Elite Software Engineer with Proven Impact at Scale',
    heroDescription: 'SDE II @ Amazon Web Services | 4+ Years | M.S. Computer Science (3.82 GPA) | AWS Certified | 20M+ LOC Delivered',
    projectsTitle: 'Career Highlights & Impact',
    aboutContent: `
      <p><strong>🎯 Current Role:</strong> Software Development Engineer II at Amazon Web Services (Promoted 2024)</p>
      
      <p><strong>💼 Professional Impact:</strong></p>
      <ul style="margin-left:2rem;margin-top:1rem;line-height:2">
        <li><strong>Led AWS Transform:</strong> Architected the world's first agentic AI platform for mainframe modernization</li>
        <li><strong>Enterprise Scale:</strong> Delivered 20M+ lines of code transformations for 23+ Fortune 500 companies</li>
        <li><strong>Innovation:</strong> Built production Claude AI-based refactoring service processing millions of LOC</li>
        <li><strong>Performance:</strong> Achieved 98.8% reduction in database restore time (4 hours → 3 minutes)</li>
        <li><strong>Reliability:</strong> Maintained 99.99% uptime while handling 12+ P1 incidents weekly as on-call engineer</li>
      </ul>
      
      <p><strong>🎓 Education:</strong></p>
      <ul style="margin-left:2rem;margin-top:0.5rem;line-height:2">
        <li>M.S. Computer Science - University of Texas at Dallas (GPA: 3.82/4.0)</li>
        <li>B.E. Computer Engineering - Gujarat Technological University (GPA: 8.46/10)</li>
      </ul>
      
      <p><strong>🏆 Certifications:</strong> AWS Certified Solutions Architect - Associate (Dec 2024)</p>
      
      <p><strong>💡 Core Competencies:</strong></p>
      <ul style="margin-left:2rem;margin-top:0.5rem;line-height:2">
        <li>Agentic AI & LLM Integration (Claude, Amazon Bedrock)</li>
        <li>Distributed Systems & Cloud Architecture (AWS CDK, Multi-region)</li>
        <li>Full-Stack Development (Java, Python, TypeScript, Scala)</li>
        <li>Database Systems (PostgreSQL, Oracle, DynamoDB)</li>
        <li>DevOps & Infrastructure (Docker, Kubernetes, Terraform)</li>
      </ul>
      
      <p><strong>📈 Career Trajectory:</strong> Rapid promotion from SDE I to SDE II in 2.5 years. Consistently delivered high-impact projects exceeding expectations. Recognized as technical leader in AI-driven modernization.</p>
    `,
    icon: '💼'
  },
  stalker: {
    heroTitle: 'The Legend Who Codes While Others Sleep',
    heroDescription: 'Elite Problem Solver | Production Hero | AWS Wizard | Coffee-Powered Code Machine',
    projectsTitle: 'Epic Builds & War Stories',
    aboutContent: `
      <p>🕵️ Alright, you caught me. Let me tell you who I really am...</p>
      
      <p><strong>The Real Deal:</strong> I'm that engineer who's saved production more times than I can count. While everyone's asleep, I'm the one debugging critical issues, deploying hotfixes, and making sure 23+ Fortune 500 companies don't wake up to disasters.</p>
      
      <p><strong>🔥 War Stories:</strong></p>
      <ul style="margin-left:2rem;margin-top:1rem;line-height:2">
        <li>Reduced DB restore from 4 hours to 3 minutes (yes, really)</li>
        <li>Built an AI that refactors 10M+ lines of code (it works, I swear)</li>
        <li>Handle 12 P1 incidents per week and still have time for memes</li>
        <li>My code runs on systems processing billions of transactions</li>
        <li>Deployed to production on a Friday (and lived to tell the tale)</li>
      </ul>
      
      <p><strong>☕ Daily Routine:</strong></p>
      <ul style="margin-left:2rem;margin-top:1rem;line-height:2">
        <li>Coffee consumption: Industrial levels</li>
        <li>Slack status: "In a meeting" (actually coding)</li>
        <li>Debugging method: console.log() everywhere (don't judge)</li>
        <li>GitHub contributions: Green squares for days</li>
        <li>Stack Overflow: Read-only mode (I answer in production)</li>
      </ul>
      
      <p><strong>🎮 Achievement Unlocked:</strong> Built systems that transform enterprise software at scale, survived countless on-call rotations, and still love what I do.</p>
      
      <p><strong>📍 Location:</strong> Dallas, TX (but my code runs everywhere)</p>
      <p><strong>🎯 Mission:</strong> Making legacy code cool again, one AI agent at a time.</p>
    `,
    icon: '🕵️'
  },
  adventurer: {
    heroTitle: 'The Epic Saga of a Code Warrior',
    heroDescription: 'Quest: Conquer Legacy Systems | Class: AI Architect | Guild: Amazon Web Services | Level: 99',
    projectsTitle: 'Legendary Quests',
    aboutContent: `
      <p>🗺️ <strong>Welcome, Traveler! This is my epic journey...</strong></p>
      
      <p><strong>⚔️ Chapter I: The Beginning (2015-2019)</strong></p>
      <p>Born in the ancient lands of Gujarat, India. Trained in the sacred arts of Computer Engineering. Defeated the final boss: Bachelor's Degree. Unlocked skill: Problem Solving.</p>
      
      <p><strong>🏰 Chapter II: The Quest for Knowledge (2020-2022)</strong></p>
      <p>Journeyed across the seas to Dallas, Texas. Entered the legendary University of Texas at Dallas. Mastered advanced Computer Science techniques. Boss defeated: Master's Thesis (Critical Hit: 3.82 GPA). Unlocked skill: Advanced Algorithms.</p>
      
      <p><strong>☁️ Chapter III: The AWS Saga (2022-Present)</strong></p>
      <p>Joined the elite guild of Amazon Web Services. Current rank: Software Development Engineer II. Main quest: Building AI systems that transform the digital realm.</p>
      
      <p><strong>🏆 Legendary Achievements Unlocked:</strong></p>
      <ul style="margin-left:2rem;margin-top:1rem;line-height:2">
        <li>⚔️ <strong>Dragon Slayer:</strong> Defeated 20M+ lines of legacy COBOL code</li>
        <li>🛡️ <strong>Guardian:</strong> Protected 23+ enterprise fortresses from modernization failures</li>
        <li>🔮 <strong>Archmage:</strong> Mastered the dark arts of LLM orchestration and multi-agent systems</li>
        <li>⚡ <strong>Speed Demon:</strong> Reduced dungeon clear time by 98.8% (4 hours → 3 minutes)</li>
        <li>☁️ <strong>Cloud Master:</strong> Conquered the AWS realm (Certified Solutions Architect)</li>
        <li>🤖 <strong>AI Whisperer:</strong> Built the first agentic AI platform for enterprise transformation</li>
      </ul>
      
      <p><strong>🎒 Inventory (Tech Stack):</strong></p>
      <ul style="margin-left:2rem;margin-top:1rem;line-height:2">
        <li>⚔️ Legendary Weapons: Java, Python, TypeScript, Scala</li>
        <li>🛡️ Armor: AWS CDK, Docker, Kubernetes</li>
        <li>🔮 Magic: Claude AI, Amazon Bedrock, FastAPI</li>
        <li>📜 Scrolls: PostgreSQL, Oracle, DynamoDB</li>
      </ul>
      
      <p><strong>🎯 Current Quest:</strong> Building agentic AI systems that make legacy code modernization not just possible, but legendary. The adventure continues...</p>
      
      <p><strong>💪 Power Level:</strong> Over 9000 (and counting)</p>
    `,
    icon: '🚀'
  }
};

// Particles animation for start screen - ENHANCED
function initParticles() {
  const canvas = document.getElementById('particlesCanvas');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  
  const particles = [];
  const particleCount = 150;
  
  // Create particles with varying properties
  for (let i = 0; i < particleCount; i++) {
    particles.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 1,
      vy: (Math.random() - 0.5) * 1,
      size: Math.random() * 3 + 1,
      opacity: Math.random() * 0.5 + 0.2,
      hue: Math.random() * 20 // Red hues
    });
  }
  
  // Mouse interaction
  let mouse = { x: null, y: null, radius: 150 };
  
  canvas.addEventListener('mousemove', (e) => {
    mouse.x = e.x;
    mouse.y = e.y;
  });
  
  canvas.addEventListener('mouseleave', () => {
    mouse.x = null;
    mouse.y = null;
  });
  
  function animate() {
    // Fade effect
    ctx.fillStyle = 'rgba(0, 0, 0, 0.05)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    
    particles.forEach((p, i) => {
      // Update position
      p.x += p.vx;
      p.y += p.vy;
      
      // Bounce off edges
      if (p.x < 0 || p.x > canvas.width) p.vx *= -1;
      if (p.y < 0 || p.y > canvas.height) p.vy *= -1;
      
      // Mouse interaction - repel particles
      if (mouse.x && mouse.y) {
        const dx = mouse.x - p.x;
        const dy = mouse.y - p.y;
        const distance = Math.sqrt(dx * dx + dy * dy);
        
        if (distance < mouse.radius) {
          const force = (mouse.radius - distance) / mouse.radius;
          const angle = Math.atan2(dy, dx);
          p.vx -= Math.cos(angle) * force * 0.5;
          p.vy -= Math.sin(angle) * force * 0.5;
        }
      }
      
      // Damping
      p.vx *= 0.99;
      p.vy *= 0.99;
      
      // Draw particle
      ctx.fillStyle = `hsla(${p.hue}, 100%, 50%, ${p.opacity})`;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
      ctx.fill();
      
      // Draw connections
      particles.forEach((p2, j) => {
        if (i === j) return;
        const dx = p.x - p2.x;
        const dy = p.y - p2.y;
        const distance = Math.sqrt(dx * dx + dy * dy);
        
        if (distance < 100) {
          ctx.strokeStyle = `rgba(229, 9, 20, ${(1 - distance / 100) * 0.2})`;
          ctx.lineWidth = 0.5;
          ctx.beginPath();
          ctx.moveTo(p.x, p.y);
          ctx.lineTo(p2.x, p2.y);
          ctx.stroke();
        }
      });
    });
    
    requestAnimationFrame(animate);
  }
  animate();
  
  // Resize handler
  window.addEventListener('resize', () => {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  });
}

// Profile background animation
function initProfileBg() {
  const canvas = document.getElementById('profileBg');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  
  let time = 0;
  function animate() {
    ctx.fillStyle = 'rgba(20, 20, 20, 0.1)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    
    for (let i = 0; i < 3; i++) {
      const x = canvas.width / 2 + Math.sin(time * 0.001 + i) * 200;
      const y = canvas.height / 2 + Math.cos(time * 0.001 + i) * 200;
      
      const gradient = ctx.createRadialGradient(x, y, 0, x, y, 200);
      gradient.addColorStop(0, 'rgba(229, 9, 20, 0.1)');
      gradient.addColorStop(1, 'transparent');
      
      ctx.fillStyle = gradient;
      ctx.fillRect(0, 0, canvas.width, canvas.height);
    }
    
    time++;
    requestAnimationFrame(animate);
  }
  animate();
}

// Netflix tudum sound - ULTIMATE VERSION
function playTudum() {
  try {
    // Create audio context
    const AudioContext = window.AudioContext || window.webkitAudioContext;
    const audioContext = new AudioContext();
    
    // Resume context if suspended (browser autoplay policy)
    if (audioContext.state === 'suspended') {
      audioContext.resume();
    }
    
    const now = audioContext.currentTime;
    
    // Create master gain for overall volume control
    const masterGain = audioContext.createGain();
    masterGain.connect(audioContext.destination);
    masterGain.gain.setValueAtTime(0.3, now);
    
    // Create the iconic TUDUM sound
    const createNote = (freq, startTime, duration, volume, type = 'sine') => {
      const osc = audioContext.createOscillator();
      const gain = audioContext.createGain();
      const filter = audioContext.createBiquadFilter();
      
      // Connect: oscillator -> filter -> gain -> master
      osc.connect(filter);
      filter.connect(gain);
      gain.connect(masterGain);
      
      // Configure oscillator
      osc.type = type;
      osc.frequency.setValueAtTime(freq, startTime);
      
      // Configure filter for warmth
      filter.type = 'lowpass';
      filter.frequency.setValueAtTime(2000, startTime);
      filter.Q.setValueAtTime(1, startTime);
      
      // Envelope
      gain.gain.setValueAtTime(0, startTime);
      gain.gain.linearRampToValueAtTime(volume, startTime + 0.01);
      gain.gain.exponentialRampToValueAtTime(0.001, startTime + duration);
      
      osc.start(startTime);
      osc.stop(startTime + duration);
    };
    
    // TU (first hit) - Deep bass
    createNote(110, now + 0.1, 0.8, 0.8, 'sine');        // A2
    createNote(220, now + 0.1, 0.8, 0.4, 'sine');        // A3
    createNote(55, now + 0.1, 0.8, 0.3, 'triangle');     // Sub bass
    
    // DUM (second hit) - Even deeper
    createNote(82.41, now + 0.5, 1.8, 1.0, 'sine');      // E2
    createNote(164.81, now + 0.5, 1.8, 0.5, 'sine');     // E3
    createNote(41.20, now + 0.5, 1.8, 0.4, 'triangle');  // Sub bass
    
    console.log('🔊 TUDUM! Audio context state:', audioContext.state);
    
    // Visual feedback
    setTimeout(() => {
      console.log('🎵 Sound should be playing now!');
    }, 100);
    
  } catch (e) {
    console.error('❌ Audio error:', e);
    // Show visual feedback instead
    const startScreen = document.getElementById('startScreen');
    if (startScreen) {
      startScreen.style.animation = 'pulse 0.5s ease-in-out';
    }
  }
}

// Initialize
initParticles();

// Start button
document.getElementById('startButton').addEventListener('click', async (e) => {
  const button = e.currentTarget;
  
  // Create ripple effect
  const ripple = document.createElement('div');
  ripple.style.cssText = `
    position: absolute;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.6);
    width: 20px;
    height: 20px;
    pointer-events: none;
    transform: translate(-50%, -50%);
    animation: ripple 0.6s ease-out;
  `;
  
  const rect = button.getBoundingClientRect();
  ripple.style.left = (e.clientX - rect.left) + 'px';
  ripple.style.top = (e.clientY - rect.top) + 'px';
  
  button.appendChild(ripple);
  
  // Add ripple animation
  const style = document.createElement('style');
  style.textContent = `
    @keyframes ripple {
      to {
        transform: translate(-50%, -50%) scale(20);
        opacity: 0;
      }
    }
  `;
  document.head.appendChild(style);
  
  setTimeout(() => ripple.remove(), 600);
  
  // Disable button
  button.disabled = true;
  button.style.opacity = '0.7';
  
  // Fade out start screen with scale effect
  const startScreen = document.getElementById('startScreen');
  startScreen.style.transition = 'opacity 0.8s ease-out, transform 0.8s ease-out';
  startScreen.style.opacity = '0';
  startScreen.style.transform = 'scale(1.1)';
  
  // Play sound after a brief moment
  setTimeout(() => {
    playTudum();
  }, 300);
  
  // Show intro after fade
  setTimeout(() => {
    startScreen.style.display = 'none';
    document.getElementById('intro').classList.add('active');
  }, 800);
  
  // After intro animation, show profile selection
  setTimeout(() => {
    document.getElementById('intro').style.display = 'none';
    document.getElementById('profileSelection').classList.add('active');
    initProfileBg();
  }, 4300);
});

// Profile selection
document.querySelectorAll('.profile-card').forEach(card => {
  card.addEventListener('click', () => {
    const profile = card.dataset.profile;
    const data = content[profile];
    
    document.getElementById('heroTitle').textContent = data.heroTitle;
    document.getElementById('heroDescription').textContent = data.heroDescription;
    document.getElementById('projectsTitle').textContent = data.projectsTitle;
    document.getElementById('aboutContent').innerHTML = data.aboutContent;
    document.getElementById('profileIconSmall').textContent = data.icon;
    
    document.getElementById('profileSelection').style.display = 'none';
    document.getElementById('mainContent').classList.add('active');
  });
});

// Header scroll
window.addEventListener('scroll', () => {
  const header = document.querySelector('.netflix-header');
  if (window.scrollY > 50) {
    header.classList.add('scrolled');
  } else {
    header.classList.remove('scrolled');
  }
});

// Smooth scroll
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function (e) {
    e.preventDefault();
    const target = document.querySelector(this.getAttribute('href'));
    if (target) target.scrollIntoView({ behavior: 'smooth' });
  });
});

// Button actions
document.querySelector('.btn-play')?.addEventListener('click', () => {
  document.getElementById('projects').scrollIntoView({ behavior: 'smooth' });
});

document.querySelector('.btn-info')?.addEventListener('click', () => {
  document.getElementById('about').scrollIntoView({ behavior: 'smooth' });
});

// Profile icon - back to selection
document.getElementById('profileIconSmall')?.addEventListener('click', () => {
  document.getElementById('mainContent').classList.remove('active');
  setTimeout(() => {
    document.getElementById('mainContent').style.display = 'none';
    document.getElementById('profileSelection').style.display = 'flex';
  }, 300);
});
